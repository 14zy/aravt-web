// appClose.ts
import { tg } from './telegramInit';

// Завершение работы приложения и закрытие окна
export const closeApp = (): void => {
    tg.close();
};

// Пример использования: закрытие приложения
// closeApp(); // Закомментировано для безопасности, вызов можно выполнить в нужный момент
